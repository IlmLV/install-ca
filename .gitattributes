# Normalize line endings: LF in repository
* text=auto eol=lf

# Shell scripts: LF always
*.sh text eol=lf

# PowerShell scripts: CRLF on checkout
# Note: `UTF-8-BOM` working-tree-encoding is not supported by all iconv builds.
*.ps1 text eol=crlf
